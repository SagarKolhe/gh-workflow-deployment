# GitHub Pages Deployment Workflow

This project demonstrates a simple CI/CD setup using **GitHub Actions** to deploy a static website to **GitHub Pages**.

Whenever changes are made to the `index.html` file and pushed to the `main` branch, the GitHub Actions workflow automatically deploys the updated site to GitHub Pages.

### URL
Once deployed, visit:
